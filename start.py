# бесконечный цикл
# стоп слова для выхода
# стрелки вверх и вниз
# реализация истории
# автор файл

# вопросы:
	# зачем нужны ЭНВ переменные?






парсинг самой строки первой
allowed_char_string = (
#  = ? +=/-* )( , ][ _ | * 
# forbidden = re.compile(pattern=rf"[^\d\w{allowed}]")